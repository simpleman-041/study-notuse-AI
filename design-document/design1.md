2つの変数を作り、int.Parse(Console.ReadLine())する
記号入力の指示を与える。半角+-＊/ののいずれか
switchで四つを選ばせる。
この時、割り算かつわるかずが0の時、エラー表示。
もとに計算する。
出力する。
もう一度やるなら再び上から

読み取り用のクラス。
計算をしてそれを返す用のクラス。
その二つのクラスをまとめるクラスを作ろう。まとめるクラスでは表示、繰り返しを加えて担当する。